<%

const sym_name = sym.name.trim();

const print_parameters = () =>
  (!sym.params
    ? ''
    : (`Parameter | Type
        --------- | ----` +
        sym.params.reduce((str, p) =>
          str + `\n${p.name} | ${p.type}`,
            '')));

const print_examples = () =>
  (sym.examples
    ? sym.examples.join('\n')
    : '');

const print_deprecation_warning = () =>
  (sym.deprecated === null
    ? ''
    : `<aside class="warning"><strong>DEPRECATED</strong><br>${sym.deprecated}</aside>`)
%>


## <%- sym_name %>

<%- print_deprecation_warning() %>

<%- sym.description %>

<%- print_parameters() %>

Returns `<%- sym.returns %>`.

<%- print_examples() %>
